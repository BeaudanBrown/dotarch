# WM binds
TOGGLE=toggleWinWorkbspwm
# Close node
super + q
  bspc node -c
# Select node in direction
super + {h,j,k,l}
  bspc node -f {west,south,north,east}
super + {shift + space,shift + f}
  bspc node -t '~{floating,fullscreen}'
# Resize node in the given direction.
super + shift + {y,u,i,o}
  node_resize {west,south,north,east} 50
# Move floating windows with the arrow keys
super + shift + {y,u,i,o}
  bspc node -v {-20 0,0 20,0 -20,20 0}
# Swap current window with the biggest one in the present desktop
super + shift + b
  bspc node -s biggest.local
# Quit/restart bspwm
super + shift + {q,r}
  {close_bspwm,bspc wm -r}
# Toggle between most recent workspaces
super + Tab
  bspwm_dynamic_desktops --df $(bspc query -D -d --names)
super + shift + {h,j,k,l}
  d={west,south,north,east}; bspc node -s $d.local --follow || bspc node -m $d --follow
super + {_,shift + ,ctrl + shift +,ctrl +}{0-9}
  ;bspwm_dynamic_desktops {--df,--ns,--nm,--da} {0-9}

# Basic binds
super + Return
  $TERMINAL
super + shift + Return
  samedir
super + space
  dmenu_run
super + r
  $TERMINAL -e $FILE -c vs
super + f
  toggleWinWorkbspwm "caprine" "💬"
super + c
  toggleWinWorkbspwm "signal-desktop" "💬" "Signal"
super + m
  toggleWinWorkbspwm "spotify" "🎶"
super + d
  toggleWinWorkbspwm "discord" "💬"
super + shift + g
  toggleWinWorkbspwm "steam" "🎮"
super + shift + m
  lmc mute
super + shift + p
  dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.PlayPause &>/dev/null
super + comma
  dbus-send --print-reply  --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Previous &>/dev/null
super + period
  dbus-send --print-reply  --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Next &>/dev/null
super + i
  $TERMINAL -e htop
super + shift a
  switchaudio
super + a
  $TERMINAL -e pulsemixer; pkill -RTMIN+10 i3blocks
super + w
  $BROWSER

# Et cetera...
super + grave
  dmenuunicode
super + Insert
  showclip
super + shift + x
  prompt "Shutdown computer?" "sudo -A shutdown -h now"
super + shift + BackSpace
  prompt "Reboot computer?" "sudo -A shutdown -r now"
super + x
  mpc pause; pauseallmpv; i3lock -e -f -c 1d2021; xset dpms force off
XF86Launch1
  xset dpms force off
XF86AudioMute
  lmc mute
XF86AudioLowerVolume
  lmc down 5
shift+XF86AudioLowerVolume
  lmc down 10
control+XF86AudioLowerVolume
  lmc down 1
XF86AudioRaiseVolume
  lmc up 5
shift+XF86AudioRaiseVolume
  lmc up 10
control+XF86AudioRaiseVolume
  lmc up 1
XF86AudioNext
  lmc next
XF86AudioPlay
  lmc toggle
XF86AudioPrev
  lmc prev
XF86AudioStop
  lmc toggle
XF86MonBrightnessDown
  xbacklight -dec 15
XF86MonBrightnessUp
  xbacklight -inc 15


# Recording
# Take screenshot
Print
  maim pic-full-$(date '+%y%m%d-%H%M-%S').png
# Pick screenshot type
shift + Print
  maimpick

# Increase volume
super + equal
  lmc up 5
# Decrease volume
super + minus
  lmc down 5

# Function keys
# Show readme
super + F1
  groff -mom $HOME/.local/share/larbs/readme.mom -Tpdf | zathura -
# F2 is restart in i3 right now.
# Change display
super + F3
  displayselect
# Hibernate
super + F4
  prompt 'Hibernate computer?' 'sudo systemctl suspend'
# Restart/rescan wifi/eth networks
super + F5
  sudo -A systemctl restart NetworkManager
# Start torrent daemon/open interface
super + F6
  torwrap
# toggle torrent daemon
super + F7
  td-toggle
# sync email
super + F8
  mailsync
# Mount a USB drive or Android device
super + F9
  dmenumount
# Unmount a USB drive or Android device
super + F10
  dmenuumount
# Network Manager interface
super + F12
  $TERMINAL -e sudo -A nmtui


